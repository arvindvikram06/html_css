## Pure CSS Carousel

## Description

A simple image carousel built using **HTML and CSS only**.
It supports both **manual sliding** and **auto sliding** without using JavaScript.

## Features

* Manual sliding using the `:checked` pseudo-class with hidden radio buttons
* Auto sliding using CSS `@keyframes`
* Smooth transitions between slides
