## Description
- This project implements a responsive multi-level dropdown menu using HTML and CSS.

- The navigation bar contains a main dropdown and a nested submenu. The dropdown appears on hover with smooth animations using opacity, transform, and transition.

- The design is also responsive. On smaller screens, the layout changes to a vertical structure using media queries.
